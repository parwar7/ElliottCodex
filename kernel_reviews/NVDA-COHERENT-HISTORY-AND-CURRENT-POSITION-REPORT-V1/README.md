# Coherent NVDA history and conditional position

Open report.html offline, or read report.md. This is a new answer-first companion to the preserved targeted-search report, not a replacement of its ledger and not another candidate experiment.

New work: factual inspection of all 333 saved Monthly bars in four declared bands, plus the ten M1/M2 role windows; Arabic synthesis of those historical observations, the exact monthly-to-daily interior path, and independent local readings. No new candidate evaluation, endpoint replacement, certificate or shared contract.

Read `NVDA_ANALYSIS_PROGRESS.md` at the repository root before resuming. Do not repeat completed search/capture stages. This pack's `synthesis.json` is a report export, not a factory-issued authority object.

## Reuse and compatibility

Prior pack: `kernel_reviews/NVDA-TARGETED-RECENT-PATH-SEARCH-AND-SYNTHESIS-V1/`, manifest `505f9561943ab942884d0cc6ebbda8d6dfca5cfe8065da246766f66224126400`. Its full canonical ledger, indicators, links, rejected alternatives and search exclusions remain authoritative evidence for their original scope. All artifacts and original snapshots are referenced in place, not overwritten/copied wholesale.

The new report presents seven explicit reader cases, not newly selected candidates or ranked results. Other surviving cases remain in the preceding report. Original aliases, endpoints, outcomes and contextual links are preserved; informational copies cannot restore live certification authority.

Historical path minima/maxima are bar-window diagnostics. A minimum lower than a proposed endpoint does not become a new Elliott rule or a substituted orthodox endpoint. Aggregate bar labels are not exact intrabar extrema times. P004/P005 and all freezes remain unchanged; inventories 11/7/0/0.

## Replay

From Runtime_WORKSPACE with the existing Python environment:

```powershell
$env:PYTHONPATH='src;tools;tests'
$env:PYTHONDONTWRITEBYTECODE='1'
$env:PYTHONIOENCODING='utf-8'
python -B tools/nvda_coherent_report.py --output artifacts/coherent-report-replay
python -B -m unittest test_nvda_coherent_report
```

Use a new empty output directory inside Runtime. The helper verifies previous/protected manifests, reads saved evidence and writes exclusively; it does not run methodology or capture data. A sealed REVIEW_manifest destination is refused. Package its artifact.json using the existing Data Analytics deliver_portable_artifact.mjs; keep temporary files inside Runtime. No extra chart framework or browser installation.

## Report and chart contract

Audience is the user, in Arabic; native portable HTML plus Markdown companion continues the authorized file-based reporting workflow. Reading order: summary, definitions, full-history context, M1/M2, historical tension, linked daily component, local alternatives, evidence gaps/coverage, indicator limitations, open questions/source trace. Required executive findings/recommendation/caveat roles are localized; technical inventories stay in supporting artifacts.

Four native scatter time/price views reuse the existing chart projection: Monthly M1, Monthly M2, Daily interior, and local 4H. Repetition is deliberate: same relationship, different isolated scenario/resolution; no invented continuous polyline between alternatives. Each includes saved closes through the last bar plus exact proposed labels, log10 price display only. Exact values remain in tables/source datasets. Palette/reader styles are the unchanged native builder defaults, with label/field distinctions independent of color. Full-width single-column delivery. Browser QA is attempted by the packaged verifier; unavailable visual checks are reported honestly.

Tests focus on new observational extraction and report isolation. The existing 1,296-test full pass is preserved in the prior baseline; this report-only stage does not modify existing executable behavior or rerun that expensive suite. Integrity and focused results are recorded here. No physical immutability or project-manager approval is claimed.
