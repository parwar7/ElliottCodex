# Development receipts and narrow corrections

The bounded experiment executed once. Selection plan and all candidate indices were saved before new evaluations. All 154 scopes and 243 routed links completed without time or linkage-cap exhaustion. No expensive pipeline rerun was performed for rendering.

The initial focused test incorrectly expected unresolved-input reasons in an otherwise fully supplied **cardinality-only** result. Inspection showed CURRENT_SUPPLIED_SCOPE_REVIEWED with exactly the cardinality behavior executed and family_validity=false. The test was corrected to verify that exact limited authority, not to alter the existing evaluator. No Kernel defect or semantic repair occurred.

The report packager required executable query provenance; the renderer now uses a real SQLite projection of captured closes and separately labelled original endpoints. This is display orchestration only. Three relevant chart regression tests passed after the change.

The final audit identified misleading generic wording in the exported open-tail reason: it could be read as denying a genuine bound developing geometric endpoint. The original flags were already correct. The reason was corrected in the report metadata only to retain those flags while denying active Elliott-wave/completion authority. The Arabic report now explicitly discusses the four contextual 4H corrective hypotheses sharing the genuine 8 September low 229.06. No price, hypothesis, role, binding, P004/P005 result, link, selection or count changed. Canonical open_tails reason strings alone were reconciled with the corrected formatter. A regression test covers this distinction.

One full-suite attempt was stopped before completion to make that report-only clarification; its partial log is preserved as full_tests_interrupted.log and is not a passing test result. The final completed full run is recorded separately in full_tests.json/full_tests.log. The final focused run is in focused_final_tests.json (57 tests); the earlier focused receipt is retained as development history.

The repeated preflight demand expressions were extracted without changing conditions, then checked against the frozen 154-scope selection ledger and aggregate-budget tests. No new authority or shared contract was introduced.

Codex Process Jobs was not used because its installed controller explicitly rejects Windows. The normal local execution path was used with Runtime-confined temporary paths. No packages, browsers, providers or MCP configuration were installed/changed.
