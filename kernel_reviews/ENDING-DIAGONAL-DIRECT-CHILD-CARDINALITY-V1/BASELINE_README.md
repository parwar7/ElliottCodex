# ENDING-DIAGONAL-DIRECT-CHILD-CARDINALITY-V1

This additive logical methodology baseline records `ENDING_DIAGONAL_DIRECT_CHILD_CARDINALITY` after protected-source confirmation, semantics lock, implementation, adversarial audit, hardening, and complete regression testing.

For an explicitly proposed `ENDING_DIAGONAL` candidate carried by one exact `OrderedChildBinding`, exactly five direct ordered children satisfies the cardinality prerequisite. Any other resolved direct-child count violates it and is fatal to that scoped candidate. Missing or malformed input and unsupported scope remain unresolved and nonfatal.

No dedicated protected P### principle exists, so `source_principle_id` remains null. The behavior is classified `SOURCE_DEFINITION` and executes as `HARD_VALIDATION`.

Satisfaction proves cardinality only. It does not validate wave labels, motive or corrective families, 3-3-3-3-3 composition, leading-diagonal semantics, wedge geometry, overlap, convergence or divergence, truncation, completion, allowed position, degree, or full diagonal validity.

The behavior is the seventh structural-invalidity producer through the existing additive registration hook. The certification algorithm, public certificate semantics, identity and attestation rules, digest semantics, sealing model, prior six producer behaviors, and validated-family machinery are unchanged.

This is logical development provenance, not physical sealing, cryptographic signing, hostile-process isolation, or immutable storage.
