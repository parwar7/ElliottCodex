# Boundary and evidence audit

- Extraction is real historical six-field OHLCV, not screenshots or latest
  quote substitution. The native series is BATS:NVDA/Cboe One, regular session.
- Every normalized price is checked against saved JSON. Native timestamps and
  raw snapshot hashes survive normalization. Numeric OHLCV converts to the
  existing float Bar contract; source decimal lexical precision is not claimed.
- Monthly and weekly timestamps are returned bar labels, not inferred first
  trade/IPO dates. All final capture bars were forming at their respective
  snapshot times. Later revisions are separately retained and explained.
- Twenty-three later data-window sample contexts (3 per resolution plus 5
  Daily turning regions) match exact historical raw rows, or recorded revisions
  of the originally forming last bar. All chart-formatted cells match their
  displayed decimal precision. Display rounding is not a methodology tolerance.
- One initial 15m sample had rolled out of the freshly loaded prefix after new
  bars arrived. One additional ordinary history batch restored it for the
  `_completed` receipt. The initial receipt is retained. No frozen rows replaced.
- Daily RSI, MACD and Volume are actual study plots with returned settings.
  Warmup nulls remain null. The initial hidden RSI produced unavailable data-window
  text although plot values existed; its visibility was enabled only on the
  temporary chart and the later verified receipt contains readable values.
  Initial and later captures are separate, not silently reconciled as simultaneous.
- Captures use the exact authorized temporary tab with Autosave visibly checked
  off. Original NVDA and MSFT symbol/resolution/range/drawing counts were checked
  read-only after capture. Original drawing contents were not re-exported or edited.
- Manual screenshot inspection covered the six recent resolution views and
  the Daily April-7 turning-bar view. Other early/middle frames are retained
  as capture receipts; numerical reconciliation does not claim manual inspection
  of every pixel. Screenshots are never executable operands.
- Existing public factories alone issue candidate/hypothesis results and P004
  certificates. Runtime does not compare Elliott measurements or issue certificates.
  The existing bounded runner audits exact role/binding/observation identities;
  representative report endpoints are independently matched to frozen rows.
- P005 established sufficiency in three Monthly-context child occurrences which
  P004 rejected. All three remain fatal locally. No family is promoted from
  another family's failure. No Preferred/Alternative ranking is assigned.
- The bounded runner keeps its existing latest-six-pivot policy. It is not the
  prior full-history or selected-scale experiment. It does not apply a new swing
  significance filter; zero/opposing role movements honestly remain unresolved.
- The L1/L2 region revisit is an analyst report, NOT factory-issued methodology
  input. Repeated A/B context links are retained. Digital extrema refine visual
  ranges but do not certify orthodox endpoints, completion, wave degree or family.
- Source policy explicitly separates source authority from the approved P005
  project measurement conventions. No indicator confirmation rules, tolerance,
  terminal wave, P006 repair, Flat/Triangle unfreeze or new methodology introduced.
- Old executable files, tests, shared contracts and baseline artifacts have no
  tracked diff. Added reusable files are the TradingView adapter, guarded digital
  tool registrar, frozen replay runner and their focused tests only.
- The report uses the existing canonical portable renderer. Its SQL-only chart
  gate is met with an actual in-memory SQLite query over original captured JSON,
  checked exactly against observation-factory results. Lookup tables preserve
  true Python/file provenance as Markdown blocks. No fake SQL source was claimed.
- HTML payload/structural checks passed; browser rendering/interaction remains
  explicitly unverified after the packaged Chrome environment check failed.
- Codex Process Jobs was attempted for long verification but returned
  `Unsupported platform: win32. Use macOS or Linux.` The ordinary local runner
  was used without installation or privilege changes; logs are retained.

## Next concrete blocker

Source-authority work: an approved decision on the already documented P006
orthodox-endpoint/path conflict remains necessary for complete impulse review.
This is **not** authorization to implement or reopen frozen semantics. The
terminal-family/base-case blocker also remains. Engineering improvements to
search coverage or report-browser QA cannot supply that missing authority.

No new development stage is started here.
