# EXACT-SCOPED-CHILD-SWING-SEARCH-TRANSPORT-V1

Approved implementation base: `4fe2432585202ad094e605563d76240e9ed1053e`.
This additive baseline records the user-authorized optional child search scope.
No protected writes, new methodology, ranking or family certification.

See contract.md for the exact public API and compatibility boundary, report/
for the same-capture before/after NVDA evidence, integrity JSON for all 51
protected entries, and audit/tests/inventory artifacts for verification.
The manifest hashes all new baseline artifacts except itself, plus modified/new
implementation and test files. It is a logical checkpoint, not physical sealing.

## Replay

From `C:\ElliottCodex\Runtime_WORKSPACE`, in PowerShell:

```powershell
$env:PYTHONPATH='src;tools;tests'
python -B tools/nvda_scoped_child_swing_search.py --output runs/scoped-child-replay-v1
```

Use a new output directory each time. The runner rejects an existing destination
or one outside Runtime. No installation, live retrieval or network is needed.
It verifies the previous quality baseline/result and the preserved capture at
kernel_reviews/NVDA-BOUNDED-ANALYSIS-REPORT-V1/inputs. The input manifest SHA is
bb8d73a6ace250a5d8815cd215760b210913db715fb43d6ef3327a8cb535424e.

The exact scale-2 Monthly roots start at source pivot indices 5, 24 and 49.
Each gets one Weekly child level. Parent six-pivot/span-six/no-skip/four-candidate
bounds and child generation/family/Normal Impulse caps remain as before.
One additional explicit operational bound selects one eligible child scope per
requirement. No other scales, history extension or deeper recursion is run.

Inputs were captured 6 September 2026 with latest bars 4 September; these are
historical replay observations, not a live market report. Existing provider
normalization/null-bar disclosures remain in the input provenance.

## Tests

```powershell
$env:PYTHONPATH='src;tools;tests'
python -B -m unittest test_scoped_child_swing_search test_scoped_child_swing_report
python -B -m unittest discover -s tests
```

The report tests replay saved evidence/audit only. No live network is used in
deterministic tests. Historical fixtures remain unchanged.

## Interpretation

Geometric alternation is an operational search domain, not Elliott validity.
Before/after differences reflect this scope selection on the same data, not a
new source rule. The prior 58 out-of-domain hypothesis occurrences are neither
58 unique waves nor 58 new structural invalidities. All original requirement
links and all unresolved family/endpoint/degree limitations remain explicit.

Recommended next stage: BOUNDED-CHILD-SCOPE-COVERAGE-REVIEW-V1, an engineering
decision review of unvisited eligible contexts. Multiple scopes per requirement
would require a separate, explicit transport/budget approval; it is not
implemented here. Frozen source semantics are not reopened.
