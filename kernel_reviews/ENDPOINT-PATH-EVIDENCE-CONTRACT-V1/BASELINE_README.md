# ENDPOINT-PATH-EVIDENCE-CONTRACT-V1

This additive logical Runtime baseline implements non-validating, hypothesis-bound endpoint and exact path evidence. The gate is `ENDPOINT_PATH_EVIDENCE_CONTRACT_SAFE`.

Family component roles exist only inside one exact live `ElliottFamilyEvaluationHypothesis`. Candidate boundary pivots and inclusive source bars retain exact identity. Endpoint price and HIGH/LOW kind are transported exactly; path high/low extrema and UP/DOWN/EQUAL direction are factual computations only. No boundary is snapped, refined, resampled or promoted to an orthodox Elliott endpoint.

Protected component orders are single-zigzag A-B-C, flat A-B-C, triangle A-B-C-D-E, and ending-diagonal 1-2-3-4-5. Contracting, expanding, ascending and descending triangle terminology is protected; running triangle is not established. No subtype is inferred.

Triangle C/E operands are now conditionally available, but `TRIANGLE_E_C_DIRECTION_SEMANTICS_UNRESOLVED` remains the readiness result. The source rule is not executed here. This baseline is logically write-once during development, not physically immutable.
